#include<stdio.h>
int main()
{
	int n,r,sum=0;
	printf("\n enter any number:");
	scanf("%d",&n);
	while(n>0)
	{
		r=n%10;
		sum=sum+r;
		n=n/10;
	}
	printf("\n sum of the digits of given number=%d",sum);
	return 0;
}
